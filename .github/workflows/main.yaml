name: Monorepo CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  en_logger:
    uses: ./.github/workflows/dart_package.yaml
    with:
      working_directory: .
      min_coverage: 100
      package_name: en_logger
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
